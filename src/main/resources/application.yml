spring:
  application:
    name: ai-code-review-assistant
  datasource:
    url: jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:5432/postgres?sslmode=require&user=postgres.qqfgnwrdsxrlxpoyjtox&password=${CRA_DB_PASSWORD}&prepareThreshold=0
    username: postgres.qqfgnwrdsxrlxpoyjtox
    password: ${CRA_DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET}
  expiration: 86400000

app:
  upload-dir: ./uploads
  reports-dir: ./reports